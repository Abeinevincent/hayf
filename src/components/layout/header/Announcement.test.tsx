import { getByTestId, render } from "@testing-library/react";
import Announcement from "./Announcement";

describe("Announcement", () => {
  test("renders email and donate button on larger screens", () => {
  //   const { getByText, getByRole } = render(<Announcement />);
  //   const emailText = getByText("codewithvincent@gmail.com");
  //   expect(emailText).toBeInTheDocument();
  //   const donateButton = getByText("Donate");
  //   expect(donateButton).toBeInTheDocument();
  });
  // test("renders donate message on smaller screens", () => {
  //   const { getByText } = render(<Announcement />);
  //   const donateMessage = getByText(
  //     /support our charity and mission\. donate to/i
  //   );
  //   expect(donateMessage).toBeInTheDocument();
  // });
});
